package:
  name: jupyter-vapor-widget
  version: "1.0.0"

source:
  path: ..

build:
  script: "{{ PYTHON }} -m pip install . -vv"
  noarch: python

requirements:
  build:
    - jupyterlab
    - jupyter_packaging
    - nodejs=17.9.0
    - yarn
    - python=3.9
  run:
    - python=3.9
